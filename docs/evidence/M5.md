# Evidence — M5 Browser + Queue + Recovery + Preflight

## Commits

```
259c970 feat(app): M4 Playwright adapter + locator + fixtures + diagnostics
ff6f847 feat(app): M5 QUE-001..005 Queue + Recovery + Preflight
5492692 feat(app): UI-001/UI-002 Queue control + History + attempt detail
```

## Verify

### npm --prefix apps/facebook-publisher run typecheck
- Expected: exit 0
- Actual: **exit 0**

### npm --prefix apps/facebook-publisher run test:integration -- --run playwright-services
- Expected: pass (skip nếu Chromium chưa cài)
- Actual: **pass / skip gracefully**

### npm --prefix apps/facebook-publisher run test:integration -- --run facebook-adapter
- Expected: pass
- Actual: **pass**

### npm --prefix apps/facebook-publisher run test:integration -- --run queue-recovery-preflight
- Expected: pass
- Actual: **pass**

## Acceptance theo Gate M5

- [x] Tất cả PW-*, QUE-*, UI-* đã checked.
- [x] Queue state machine chuyển trạng thái đúng; retry có exponential backoff.
- [x] Crash recovery: pre-submit → re-queue, post-submit → `unknown` (unverified).
- [x] Preflight check: token expired / out of stock / archived / price changed.
- [x] Auto-submit gated bởi `settings.auto_submit` + group `posting_mode`.

## Reviewer

- DEV-D — 2026-08-02 — Windows 11 — PowerShell 7.
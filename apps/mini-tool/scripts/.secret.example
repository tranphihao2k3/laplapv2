# LapLap Mini Tool — shared HMAC secret placeholder.
#
# Copy this file to `.secret` next to the executable at runtime and put the
# real shared secret on a single line. Used by `src/main/crypto.ts::getSecret()`
# to sign uploads sent to /api/v1/mini-tool/upload.
#
# Do NOT commit the real `.secret` file.
# Only this `.secret.example` placeholder is allowed in the repository.

dev-only-placeholder-secret-do-not-ship
LAPLAP_MINI_TOOL_SECRET=f0010cf0aa8f645f80ae7e651d40b5f310e06cdf32b330acae16668648183397
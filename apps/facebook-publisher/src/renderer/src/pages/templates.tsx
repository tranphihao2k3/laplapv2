export function TemplatesPage() {
  return (
    <section>
      <h1 className="text-lg font-semibold">Mẫu đăng</h1>
      <p className="mt-2 text-sm text-muted-500">
        Tạo mẫu đăng với biến an toàn. Tính năng sẽ thêm ở TPL-001.
      </p>
    </section>
  );
}